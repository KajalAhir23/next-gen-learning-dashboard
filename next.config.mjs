/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow opening dev site from your LAN IP (e.g. http://10.x.x.x:3000)
  allowedDevOrigins: ["10.71.19.122"],
};

export default nextConfig;
